{
  "name": "passman",
  "version": "0.1.0",
  "private": true,
  "description": "Passman — zero-knowledge password manager (monorepo)",
  "workspaces": [
    "packages/core",
    "packages/web",
    "packages/extension"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
